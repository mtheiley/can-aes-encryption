#ifndef GALOIS_TABLE_H
#define GALOIS_TABLE_H

#include <cstdint>

struct GaloisTable {
    static uint8_t gfield2(uint8_t value);
    static uint8_t gfield3(uint8_t value);
    //static uint8_t gfield9(uint8_t value);
    //static uint8_t gfield11(uint8_t value);
    //static uint8_t gfield13(uint8_t value);
    //static uint8_t gfield14(uint8_t value);
};

#endif //GALOIS_TABLE_H